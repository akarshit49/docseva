# E2E fixtures

Drop a real supplier PDF here named `supplier-quote.pdf` before running
the anchor-flow spec with `PW_RUN_E2E_FULL=1`. It must be a valid PDF
the OpenAI parser can read — any quotation from a tools/instruments
supplier works.

Not committed to git on purpose (might contain real vendor pricing).
